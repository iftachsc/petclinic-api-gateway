---
apiVersion: v1
kind: Route
metadata:
  labels:
    app: api-gateway
    provider: fabric8
    version: 1.5.9
    group: org.springframework.samples.petclinic.api
  name: api-gateway
spec:
  port:
    targetPort: http
  to:
    kind: Service
    name: api-gateway
    weight: 100
